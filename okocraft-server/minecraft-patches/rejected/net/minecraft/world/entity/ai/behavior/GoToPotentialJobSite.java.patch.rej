++++ REJECTED HUNK: 1
@@ -46,12 +46,21 @@
             BlockPos blockPos = globalPos.pos();
             ServerLevel level1 = level.getServer().getLevel(globalPos.dimension());
             if (level1 != null) {
+                Runnable run = () -> { // okocraft - region threading
                 PoiManager poiManager = level1.getPoiManager();
                 if (poiManager.exists(blockPos, holder -> true)) {
                     poiManager.release(blockPos);
                 }
 
                 DebugPackets.sendPoiTicketCountPacket(level, blockPos);
+                // okocraft start - region threading
+                };
+                if (ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor(level1, blockPos)) {
+                    run.run();
+                } else {
+                    io.papermc.paper.threadedregions.RegionizedServer.getInstance().taskQueue.queueChunkTask(level1, blockPos.getX() >> 4, blockPos.getZ() >> 4, run);
+                }
+                // okocraft end - region threading
             }
         });
         entity.getBrain().eraseMemory(MemoryModuleType.POTENTIAL_JOB_SITE);
++++ END HUNK
