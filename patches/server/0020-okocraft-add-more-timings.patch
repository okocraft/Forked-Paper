From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Siroshun09 <siro.shun8@gmail.com>
Date: Tue, 20 Sep 2022 14:16:03 +0900
Subject: [PATCH] okocraft add more timings


diff --git a/src/main/java/co/aikar/timings/WorldTimingsHandler.java b/src/main/java/co/aikar/timings/WorldTimingsHandler.java
index fe79c0add4f7cb18d487c5bb9415c40c5b551ea2..99ecf02ccc16606fda84a923f83b73e072311043 100644
--- a/src/main/java/co/aikar/timings/WorldTimingsHandler.java
+++ b/src/main/java/co/aikar/timings/WorldTimingsHandler.java
@@ -67,6 +67,26 @@ public class WorldTimingsHandler {
     public final Timing chunkUnloadPrepareSave;
     public final Timing chunkUnloadPOISerialization;
     public final Timing chunkUnloadDataSave;
+    // okocraft start - add more timings
+    public final Timing entityUpdateVariousThings;
+    public final Timing entityAiStep;
+    public final Timing livingEntityAiStepAi;
+    public final Timing livingEntityAiStepTravel;
+    public final Timing livingEntityAiStepPush;
+    
+    public final Timing serverAiStepSensing;
+    public final Timing serverAiStepTargetSelector;
+    public final Timing serverAiStepGoalSelector;
+    public final Timing serverAiStepNavigation;
+    public final Timing serverAiStepMobTick;
+    public final Timing serverAiStepMove;
+    public final Timing serverAiStepLook;
+    public final Timing serverAiStepJump;
+
+    public final Timing villagerBrainTick;
+
+    public final Timing behaviorAcquirePoiPoiAccess;
+    // okocraft end
 
     public WorldTimingsHandler(Level server) {
         String name = ((PrimaryLevelData) server.getLevelData()).getLevelName() + " - ";
@@ -131,6 +151,28 @@ public class WorldTimingsHandler {
         chunkUnloadPrepareSave = Timings.ofSafe(name + "Chunk unload - Async Save Prepare");
         chunkUnloadPOISerialization = Timings.ofSafe(name + "Chunk unload - POI Data Serialization");
         chunkUnloadDataSave = Timings.ofSafe(name + "Chunk unload - Data Serialization");
+
+        // okocraft start - add more timings
+        entityUpdateVariousThings = Timings.ofSafe(name + "Entity Update Various Things");
+        entityAiStep = Timings.ofSafe(name + "Entity AI Step");
+
+        livingEntityAiStepAi = Timings.ofSafe(name + "LivingEntity AI Step - Ai");
+        livingEntityAiStepTravel = Timings.ofSafe(name + "LivingEntity AI Step - Travel");
+        livingEntityAiStepPush = Timings.ofSafe(name + "LivingEntity AI Step - Push");
+
+        serverAiStepSensing = Timings.ofSafe(name + "Server AI Step - Sensing");
+        serverAiStepTargetSelector = Timings.ofSafe(name + "Server AI Step - Target Selector");
+        serverAiStepGoalSelector = Timings.ofSafe(name + "Server AI Step - Goal Selector");
+        serverAiStepNavigation = Timings.ofSafe(name + "Server AI Step - Navigation");
+        serverAiStepMobTick = Timings.ofSafe(name + "Server AI Step - Mob Tick");
+        serverAiStepMove = Timings.ofSafe(name + "Server AI Step - Move");
+        serverAiStepLook = Timings.ofSafe(name + "Server AI Step - Look");
+        serverAiStepJump = Timings.ofSafe(name + "Server AI Step - Jump");
+
+        villagerBrainTick = Timings.ofSafe(name + "Villager Brain Tick");
+
+        behaviorAcquirePoiPoiAccess = Timings.ofSafe(name + "Behavior AcquirePoi Poi Access");
+        // okocraft end
     }
 
     public static Timing getTickList(ServerLevel worldserver, String timingsType) {
diff --git a/src/main/java/net/minecraft/world/entity/Entity.java b/src/main/java/net/minecraft/world/entity/Entity.java
index 96db157e294ef823b25a10fa4db37a2c7cd87bfa..f1db9b25c10591014afb4ed44a8c8928a1fce3f2 100644
--- a/src/main/java/net/minecraft/world/entity/Entity.java
+++ b/src/main/java/net/minecraft/world/entity/Entity.java
@@ -820,9 +820,11 @@ public abstract class Entity implements Nameable, EntityAccess, CommandSource {
 
         this.wasInPowderSnow = this.isInPowderSnow;
         this.isInPowderSnow = false;
+        this.level.timings.entityUpdateVariousThings.startTiming(); // okocraft - add more timings
         this.updateInWaterStateAndDoFluidPushing();
         this.updateFluidOnEyes();
         this.updateSwimming();
+        this.level.timings.entityUpdateVariousThings.stopTiming(); // okocraft - add more timings
         if (this.level.isClientSide) {
             this.clearFire();
         } else if (this.remainingFireTicks > 0) {
diff --git a/src/main/java/net/minecraft/world/entity/LivingEntity.java b/src/main/java/net/minecraft/world/entity/LivingEntity.java
index bf10cb44200b9b09f23e7ecec31eb36196401fa7..32be847c5338db81fefd273e4a1b07c531f7ca8f 100644
--- a/src/main/java/net/minecraft/world/entity/LivingEntity.java
+++ b/src/main/java/net/minecraft/world/entity/LivingEntity.java
@@ -2994,9 +2994,11 @@ public abstract class LivingEntity extends Entity {
             }
         }
 
+        try (co.aikar.timings.Timing ignored = this.level.timings.entityAiStep.startTiming()) { // okocraft - add more timing
         if (!this.isRemoved()) {
             this.aiStep();
         }
+        } // okocraft - add more timings
 
         double d0 = this.getX() - this.xo;
         double d1 = this.getZ() - this.zo;
@@ -3326,6 +3328,7 @@ public abstract class LivingEntity extends Entity {
         }
 
         this.setDeltaMovement(d4, d5, d6);
+        this.level.timings.livingEntityAiStepAi.startTiming(); // okocraft - add more timings
         this.level.getProfiler().push("ai");
         if (this.isImmobile()) {
             this.jumping = false;
@@ -3336,6 +3339,7 @@ public abstract class LivingEntity extends Entity {
             this.serverAiStep();
             this.level.getProfiler().pop();
         }
+        this.level.timings.livingEntityAiStepAi.stopTiming(); // okocraft - add more timings
 
         this.level.getProfiler().pop();
         this.level.getProfiler().push("jump");
@@ -3367,14 +3371,17 @@ public abstract class LivingEntity extends Entity {
 
         this.level.getProfiler().pop();
         this.level.getProfiler().push("travel");
+        AABB axisalignedbb;
+        try (co.aikar.timings.Timing ignored = this.level.timings.livingEntityAiStepTravel.startTiming()) { // okocraft - add more timings
         this.xxa *= 0.98F;
         this.zza *= 0.98F;
         this.updateFallFlying();
-        AABB axisalignedbb = this.getBoundingBox();
+        axisalignedbb = this.getBoundingBox();
 
         // SpigotTimings.timerEntityAIMove.startTiming(); // Spigot // Paper
         this.travel(new Vec3((double) this.xxa, (double) this.yya, (double) this.zza));
         // SpigotTimings.timerEntityAIMove.stopTiming(); // Spigot // Paper
+        }  // okocraft - add more timings
         this.level.getProfiler().pop();
         this.level.getProfiler().push("freezing");
         boolean flag1 = this.getType().is(EntityTypeTags.FREEZE_HURTS_EXTRA_TYPES);
@@ -3397,6 +3404,7 @@ public abstract class LivingEntity extends Entity {
         }
 
         this.level.getProfiler().pop();
+        this.level.timings.livingEntityAiStepPush.startTiming(); // okocraft - add more timings
         this.level.getProfiler().push("push");
         if (this.autoSpinAttackTicks > 0) {
             --this.autoSpinAttackTicks;
@@ -3405,6 +3413,7 @@ public abstract class LivingEntity extends Entity {
 
         this.pushEntities();
         this.level.getProfiler().pop();
+        this.level.timings.livingEntityAiStepPush.stopTiming(); // okocraft - add more timings
         // Paper start
         if (((ServerLevel) this.level).hasEntityMoveEvent && !(this instanceof net.minecraft.world.entity.player.Player)) {
             if (this.xo != getX() || this.yo != this.getY() || this.zo != this.getZ() || this.yRotO != this.getYRot() || this.xRotO != this.getXRot()) {
diff --git a/src/main/java/net/minecraft/world/entity/Mob.java b/src/main/java/net/minecraft/world/entity/Mob.java
index c89aa33a5c7daca5cd581c5ba4b3e0ba14a18be8..0064760acaf57a814d6dbd1ae7d6d305c61fddb0 100644
--- a/src/main/java/net/minecraft/world/entity/Mob.java
+++ b/src/main/java/net/minecraft/world/entity/Mob.java
@@ -854,43 +854,63 @@ public abstract class Mob extends LivingEntity {
         }
         // Paper end
         this.level.getProfiler().push("sensing");
+        this.level.timings.serverAiStepSensing.startTiming();
         this.sensing.tick();
+        this.level.timings.serverAiStepSensing.stopTiming();
         this.level.getProfiler().pop();
         int i = this.level.getServer().getTickCount() + this.getId();
 
         if (i % 2 != 0 && this.tickCount > 1) {
             this.level.getProfiler().push("targetSelector");
+            this.level.timings.serverAiStepTargetSelector.startTiming();
             if (this.targetSelector.inactiveTick(this.activatedPriority)) // Pufferfish - use this to alternate ticking
             this.targetSelector.tickRunningGoals(false);
+            this.level.timings.serverAiStepTargetSelector.stopTiming();
             this.level.getProfiler().pop();
             this.level.getProfiler().push("goalSelector");
+            this.level.timings.serverAiStepGoalSelector.startTiming();
             if (this.goalSelector.inactiveTick(this.activatedPriority)) // Pufferfish - use this to alternate ticking
             this.goalSelector.tickRunningGoals(false);
+            this.level.timings.serverAiStepGoalSelector.stopTiming();
             this.level.getProfiler().pop();
         } else {
             this.level.getProfiler().push("targetSelector");
+            this.level.timings.serverAiStepTargetSelector.startTiming();
             if (this.targetSelector.inactiveTick(this.activatedPriority)) // Pufferfish - use this to alternate ticking
             this.targetSelector.tick();
+            this.level.timings.serverAiStepTargetSelector.stopTiming();
             this.level.getProfiler().pop();
             this.level.getProfiler().push("goalSelector");
+            this.level.timings.serverAiStepGoalSelector.startTiming();
             if (this.goalSelector.inactiveTick(this.activatedPriority)) // Pufferfish - use this to alternate ticking
             this.goalSelector.tick();
+            this.level.timings.serverAiStepGoalSelector.stopTiming();
             this.level.getProfiler().pop();
         }
 
         this.level.getProfiler().push("navigation");
+        this.level.timings.serverAiStepNavigation.startTiming();
         this.navigation.tick();
+        this.level.timings.serverAiStepNavigation.stopTiming();
         this.level.getProfiler().pop();
         this.level.getProfiler().push("mob tick");
+        this.level.timings.serverAiStepMobTick.startTiming();
         this.customServerAiStep();
+        this.level.timings.serverAiStepMobTick.stopTiming();
         this.level.getProfiler().pop();
         this.level.getProfiler().push("controls");
         this.level.getProfiler().push("move");
+        this.level.timings.serverAiStepMove.startTiming();
         this.moveControl.tick();
+        this.level.timings.serverAiStepMove.stopTiming();
         this.level.getProfiler().popPush("look");
+        this.level.timings.serverAiStepLook.startTiming();
         this.lookControl.tick();
+        this.level.timings.serverAiStepLook.stopTiming();
         this.level.getProfiler().popPush("jump");
+        this.level.timings.serverAiStepJump.startTiming();
         this.jumpControl.tick();
+        this.level.timings.serverAiStepJump.stopTiming();
         this.level.getProfiler().pop();
         this.level.getProfiler().pop();
         this.sendDebugPackets();
diff --git a/src/main/java/net/minecraft/world/entity/ai/behavior/AcquirePoi.java b/src/main/java/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
index e46a0e5a080344b5433688994e5aa138d57243b6..a2bc6391827a9d2d9957cae4a93e63c8ba290977 100644
--- a/src/main/java/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
+++ b/src/main/java/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
@@ -88,11 +88,13 @@ public class AcquirePoi extends Behavior<PathfinderMob> {
                 return true;
             }
         };
+        entity.level.timings.behaviorAcquirePoiPoiAccess.startTiming(); // okocraft - add more timings
         // Paper start - optimise POI access
         java.util.List<Pair<Holder<PoiType>, BlockPos>> poiposes = new java.util.ArrayList<>();
         io.papermc.paper.util.PoiAccess.findNearestPoiPositions(poiManager, this.poiType, predicate, entity.blockPosition(), 48, 48*48, PoiManager.Occupancy.HAS_SPACE, false, 5, poiposes);
         Set<Pair<Holder<PoiType>, BlockPos>> set = new java.util.HashSet<>(poiposes);
         // Paper end - optimise POI access
+        entity.level.timings.behaviorAcquirePoiPoiAccess.stopTiming(); // okocraft - add more timings
         Path path = findPathToPois(entity, set);
         if (path != null && path.canReach()) {
             BlockPos blockPos = path.getTarget();
diff --git a/src/main/java/net/minecraft/world/entity/npc/Villager.java b/src/main/java/net/minecraft/world/entity/npc/Villager.java
index e41f5efb250f1b4449bfee34a8d055e4a8445b50..ce93e6170e71830659406d2c7018c40074d3940d 100644
--- a/src/main/java/net/minecraft/world/entity/npc/Villager.java
+++ b/src/main/java/net/minecraft/world/entity/npc/Villager.java
@@ -248,12 +248,14 @@ public class Villager extends AbstractVillager implements ReputationEventHandler
     protected void customServerAiStep() { mobTick(false); }
     protected void mobTick(boolean inactive) {
         this.level.getProfiler().push("villagerBrain");
+        this.level.timings.villagerBrainTick.startTiming(); // okocraft - add more timings
         // Pufferfish start
         if (!inactive) {
             if (this.behaviorTick++ % this.activatedPriority == 0) // Pufferfish
             this.getBrain().tick((ServerLevel) this.level, this); // Paper
          }
         // Pufferfish end
+        this.level.timings.villagerBrainTick.stopTiming(); // okocraft - add more timings
         this.level.getProfiler().pop();
         if (this.assignProfessionWhenSpawned) {
             this.assignProfessionWhenSpawned = false;
